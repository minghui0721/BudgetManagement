# JVM arguments for Gradle build process
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError

# Set the JDK path explicitly (update to your actual JDK 17 path)
org.gradle.java.home=C:/Program Files/Java/jdk-17.0.0.1

# Enable AndroidX and Jetifier for backward compatibility with older libraries
android.useAndroidX=true
android.enableJetifier=true
